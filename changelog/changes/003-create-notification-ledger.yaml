databaseChangeLog:
  - changeSet:
      id: 003-create-notification-ledger
      author: tom.huang
      comment: Placeholder table. Columns not finalized yet — flesh out in a later changeset.
      changes:
        - createTable:
            tableName: notification_ledger
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: created_at
                  type: timestamptz
                  defaultValueComputed: now()
                  constraints:
                    nullable: false
