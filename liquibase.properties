# Used only if you run the Liquibase CLI directly. The docker-compose `liquibase`
# service passes these as flags instead, so this file is a convenience for manual
# runs (e.g. `liquibase update` from this directory).
searchPath: changelog
changeLogFile: db.changelog-master.yaml
url: jdbc:postgresql://localhost:5432/greenlight
username: greenlight
password: greenlight
driver: org.postgresql.Driver
